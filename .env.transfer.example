# ENV EXAMPLE FOR DATABASE TRANSFER SCRIPT
# NOTE: Ensure proper connection configuration as needed.

# Source database (data origin)
SOURCE_DB_URL="postgresql://USERNAME:PASSWORD@HOST/SOURCE_DB?sslmode=require&channel_binding=require"

# Target database (data destination)
TARGET_DB_URL="postgresql://USERNAME:PASSWORD@HOST/TARGET_DB"
